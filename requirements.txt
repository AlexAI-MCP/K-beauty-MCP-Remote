# K-Beauty MCP Server Dependencies

# 기존 로컬 MCP 의존성
mcp>=1.0.0
asyncio
typing
datetime
logging

# 리모트 MCP HTTP 서버 의존성
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# 선택적 의존성 (OAuth, 보안 등)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-decouple>=3.8

# 배포용 의존성
gunicorn>=21.2.0
